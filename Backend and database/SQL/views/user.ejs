<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>user</title>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body>
    <h1>This is route of all user ie. home(main route)</h1>

    <table>
      <tr>
        <th>id</th>
        <th>Name</th>
        <th>Email</th>
      </tr>
        <% for (let i = 0; i < result.length; i++) { %>
            <tr>

                        <td><%= result[i].id %></p></td>
                        <td> <%= result[i].username %></td>
                        <td><%= result[i].email %></td>
                    </tr>
        <% } %>
    </table>
  </body>
</html>
